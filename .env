CERT_PATH="E:/micro-service/micro-frontend/certs"
LOG_LEVEL=warn

# TODO: Dynamic port changes for USER service won't work unless `E:\micro-service\micro-frontend\projects\shell\public\federation.manifest.json` is handled.
SHELL_HOST="http://host.docker.internal"
SHELL_PORT=4200

USER_PORT=4210
USER_HOST="http://host.docker.internal"
