<VirtualHost *:80>
    ServerName localhost.com
    Redirect / https://localhost.com/
</VirtualHost>

<VirtualHost *:443>
    ServerName  localhost.com
    Protocols h2 http/1.1

SSLEngine on
Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains"
SSLProtocol -ALL +TLSv1.2 +TLSv1.1 +TLSv1
SSLHonorCipherOrder On
SSLCipherSuite !SSLv2:!EXPORT:!DHE+AES-GCM:!DHE+AES:!DHE+3DES:ECDHE+AES-GCM:ECDHE+AES:RSA+AES-GCM:RSA+AES:ECDHE+3DES:RSA+3DES:-MD5:SSLv3:-RC4
SSLCertificateFile /etc/ssl/certs/localhost.crt
SSLCertificateKeyFile /etc/ssl/certs/localhost.key
SSLProxyEngine on
SSLProxyVerify none
SSLProxyCheckPeerCN off
SSLProxyCheckPeerName off

Include conf/mod_headers.conf
AddDefaultCharset UTF-8

    ProxyPreserveHost on
    RewriteEngine on

    # Add the specific rule for /federation.manifest.json
    RewriteRule "^/$" "/shell/" [L,R=301]
    RewriteRule "^/shell(.*)$" "${SHELL_HOST}:${SHELL_PORT}/shell$1" [P,QSA,L]
    RewriteRule "^/user(.*)$" "${USER_HOST}:${USER_PORT}/user$1" [P,QSA,L]

</VirtualHost>
